version: "3.9"
services:
  sonarr:
    image: linuxserver/sonarr:latest
    container_name: sonarr
    network_mode: "host"
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    volumes:
      - ./config:/config
      - media_downloads:/downloads
    restart: unless-stopped

volumes:
  media_downloads:
    external: true
