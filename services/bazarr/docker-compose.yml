version: "3.9"
services:
  bazarr:
    image: linuxserver/bazarr:latest
    container_name: bazarr
    network_mode: "host"
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    volumes:
      - ./config:/config
      - media_downloads:/downloads
    restart: unless-stopped

volumes:
  media_downloads:
    external: true
