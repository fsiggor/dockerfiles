# Prowlarr

Indexer manager/proxy built on the popular *arr .net/react stack.

## Usage
```bash
docker-compose up -d
```

## Configuration
- **Port**: `9696`
- **URL**: [http://localhost:9696](http://localhost:9696)
- **Config**: `./config` (Local bind mount)

## Network
Uses `network_mode: host`.
